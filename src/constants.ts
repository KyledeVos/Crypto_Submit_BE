/**
 * @module server.ts
 * This module serves as a holder for all constant value declarations used globally in the application
 * Keep the names in full capital as a format reminder that these are constants defined in one location
 */

// DATABASE
export const DATABASE_NAME = "testdb"

// TABLES
export const COIN_TABLE = "testdb"